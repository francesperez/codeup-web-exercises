//----------------------------------------------BOM----------------------------------------------------------------
//Did you know?...The browser is also an object that can be interacted with.
//This is called the BOM: Browser Object Model
//Most things that can be manipulated or done to the BOM is done using the window. object
//the window. object can control the browser size, position, open up pop-up windows, display system dialogs, and more.



//----------------------------------------------DOM----------------------------------------------------------------
// However, when it comes to Javascript, we mostly use DOM: Document Object Model, which is part of the BOM. It is
// the ACTUAL website document. Most things within the DOM are done using the document. method